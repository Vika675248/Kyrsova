# Розгортання на Render (коротка інструкція) ✅

Це короткі кроки, щоб розгорнути проєкт як один веб-сервіс (сервер + збудований клієнт):

1. Підготувати репозиторій
   - Закомітьте зміни (включно з оновленим `package.json` та `render.yaml`).

2. Налаштувати Render
   - Створіть новий Web Service → підключіть ваш репозиторій → виберіть гілку (наприклад `main`).
   - Build Command: `npm ci && npm run build`
   - Start Command: `npm start`
   - Environment: Node (Render зазвичай встановлює `NODE_ENV=production` автоматично)

3. Додати секрети (Environment Variables)
   Обов'язкові у Render Settings → Environment:
   - `MONGODB_URI` — ваш connection string до MongoDB (Atlas або інший)
   - `JWT_SECRET` — секрет для підпису токенів
   - (Опційно) `REACT_APP_API_URL` — якщо клієнт має явно вказувати API URL
   - (Опційно) `MAX_FILE_SIZE`, `ALLOWED_FILE_TYPES`, `JWT_EXPIRE`

4. Файли завантаження (uploads)
   - За замовчуванням Render не гарантує постійність локальної файлової системи між релізами.
   - Для зберігання зображень/файлів рекомендовано використовувати S3 (або Render Persistent Disk).

5. Перевірка
   - Переконайтесь, що в консолі Render build завершився успішно.
   - Відкрийте URL сервісу в браузері — додаток має віддавати React UI і API має відповідати на `/api/*`.

6. Локальна перевірка перед пушем
   - Запустіть локально: `npm run install:all` (встановить залежності) і `npm run build` (збудує клієнт).
   - Запустіть сервер у production-подібному режимі: `NODE_ENV=production node server/index.js` (на Windows можна використовувати cross-env або встановити перемінну в PowerShell):
     - PowerShell: `$env:NODE_ENV = "production"; node server/index.js`

---

Якщо хочеш, можу:
- Автоматично додати інструкції для збереження uploads на S3 (приклади коду),
- Додати `.github/workflows` або `render.yaml` більш докладно,
- Налаштувати persist disk у коді.

🔧 Потрібно, щоб я зробив додаткові зміни (S3, workflow), чи достатньо цих кроків?